spring:
  application:
     name: gateway

  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**
server:
  port: 4004






eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
